<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.board.mapper.StoreMapper">

	<insert id="addStore">
		<selectKey keyProperty="storeId" resultType="int" order="BEFORE">
			SELECT JOONGGO_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO store(store_id, user_id)
		VALUES(#{storeId}, #{userId})
	</insert>
	
	<delete id="deleteStore">
		DELETE FROM store a
		WHERE EXISTS (
			SELECT user_id FROM site_user b 
			WHERE a.user_id = #{userId}
		)
	</delete>

</mapper>